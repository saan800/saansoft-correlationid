name: release

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
  workflow_dispatch: # 👈 manual trigger here to release
    inputs:
      force-release:
        description: "Force a release regardless of PR labels"
        type: boolean
        required: false
        default: false

permissions:
  contents: write
  packages: write
  pull-requests: read

jobs:
  ci-cd:
    permissions:
      contents: write
      packages: write
      pull-requests: read
    uses: saan800/github/.github/workflows/dotnet-package-release.yml@main
    with:
      os: ubuntu-latest
      dotnet-version: 9.0.x
      codecov-slug: "saan800/saansoft-correlationId"
      is-release-branch: ${{ github.ref == 'refs/heads/main' }}
      force-release: ${{ github.event.inputs.force-release == 'true' }}
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      NUGET_API_KEY: ${{ secrets.NUGET_TOKEN }}
